client goes with tcp connection to the server which handles connection and then stores them in redis.
Conn represents one TCP connection between the server and the client.
Handleconnection helps many clients or connections to be handled independently
